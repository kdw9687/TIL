def fib_tab(n):
    # 코드를 작성하세요.
    tab = []

    for i in range(0, n + 1, 1):
        if i == 0:
            tab.append(1)
        elif i == 1:
            tab.append(1)
        else:
            b = tab[i - 1] + tab[i - 2]
            tab.append(b)

    return tab[n - 1]


# 테스트
print(fib_tab(10))
print(fib_tab(56))
print(fib_tab(132))
